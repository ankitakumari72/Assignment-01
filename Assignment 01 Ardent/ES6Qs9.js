const utils = {
  add: (a, b) => a + b,

  subtract: (a, b) => a - b,

  multiply: (a, b) => a * b,

  divide: (a, b) => a / b
};

console.log(utils.add(10, 5));
console.log(utils.multiply(4, 3));